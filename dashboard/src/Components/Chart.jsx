const { useState } = require("react");

const DynamicChart = () => {
    const [chartData, setChartData] = useState({});
    const [employeSalary, setEmployeSalary] = useState([]);
    const [employeAge, setEmployeAge] = useState([]);
    const chart = () => { }
}

export default DynamicChart;